$(document).ready(function() {
  $('#gallery-center6').magnificPopup({ //parintele
    delegate: 'a', // selector de elemente de tip copil, dand click se va deschide popup
    type: 'image',
    // alte optiuni
    gallery: {
  // optiuni pentru galerie
  enabled: true
  }
  });
});
